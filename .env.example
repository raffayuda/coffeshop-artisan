# Database Configuration
# PostgreSQL connection string
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
DATABASE_URL="postgresql://postgres:password@localhost:5432/coffeeshop?schema=public"

# Authentication Secret
# Generate with: openssl rand -base64 32
# REQUIRED for nuxt-auth-utils session encryption
NUXT_SESSION_PASSWORD="your-32-character-secret-key-here-change-this"

# Optional: Database Connection Pool Limit
# Recommended for production: 10-20
DATABASE_CONNECTION_LIMIT="10"

# Optional: Node Environment
NODE_ENV="development"
